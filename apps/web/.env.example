# Next.js 前端环境变量（复制为 .env.local 后填写）
# 指向 NestJS 主后端的来源（不带 /api，rewrites 会自动补全 /api）
# 本地：http://localhost:3001     生产：你的 Render 后端地址，如 https://ankle-api.onrender.com
NEXT_PUBLIC_API_URL="http://localhost:3001"
